================================================================================
  THỰC HÀNH - RXJS SEARCH FLOW CHECKPOINT
================================================================================

MỤC TIÊU:
- Dùng RxJS cho UI stream thực tế: search + debounce + switchMap + error handling

THỜI LƯỢNG ƯỚC TÍNH:
- 30-40 phút

TASK:
1. Tạo 1 input search
2. Tạo searchTerm$ bằng Subject<string> hoặc FormControl.valueChanges
3. Pipe chain gồm:
   - debounceTime(300)
   - distinctUntilChanged()
   - switchMap(...)
   - catchError(...)
4. Gọi ProductService.search(term)
5. Thêm 1 bộ lọc phụ category$ hoặc sort$
6. Nếu thêm bộ lọc phụ, dùng combineLatest để tạo request state cuối cùng
7. Render kết quả qua async pipe

EXPECTED RESULT:
- Typing nhanh không spam request
- Request cũ bị hủy khi request mới bắt đầu
- Lỗi không làm vỡ stream hoàn toàn

SELF-CHECK:
- Bạn có giải thích được vì sao switchMap hợp hơn mergeMap trong search không?
- Bạn có biết async pipe giúp tránh unsubscribe thủ công ở case này không?
- Bạn có hiểu Observable đang đại diện cho event stream nào không?

DONE KHI:
- Bạn có 1 search/list flow dùng được trong app thực
================================================================================
