================================================================================
  THỰC HÀNH - SIGNAL SERVICE CHECKPOINT
================================================================================

MỤC TIÊU:
- Tạo 1 service dùng signals làm source of truth
- Dùng computed() cho derived state
- Có persistence localStorage

THỜI LƯỢNG ƯỚC TÍNH:
- 30-40 phút

================================================================================
  LỆNH CẦN NHỚ
================================================================================

  - `items = signal<Item[]>([])`
  - `filteredItems = computed(() => ...)`
  - `items.update(v => [...v, item])` để mutate state an toàn


TASK:
1. Tạo PreferencesService hoặc TodoService
2. Trong service, tạo:
   - items = signal<Item[]>([])
   - filter = signal<'all' | 'active' | 'done'>('all')
   - filteredItems = computed(...)
   - remainingCount = computed(...)
3. Viết methods:
   - addItem()
   - toggleItem()
   - removeItem()
   - clearDone()
4. Lưu state vào localStorage mỗi lần thay đổi
5. Load state từ localStorage khi app bắt đầu
6. Ở component, chỉ đọc signal và gọi methods của service

EXPECTED RESULT:
- Service là nơi giữ state trung tâm
- Component không cần tự quản lý business state trùng lặp
- Reload trang vẫn giữ được data

SELF-CHECK:
- Bạn có giải thích được vì sao filteredItems là computed thay vì mảng được update tay không?
- Bạn có biết khi nào nên đổi sang Observable không? Gợi ý: HTTP, route params, search stream
- Bạn có giữ UI logic ở component và business logic ở service không?

DONE KHI:
- Bạn có 1 signal service hoạt động giống source of truth thực tế
================================================================================
